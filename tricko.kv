#:import QRCodeWidget kivy.garden.qrcode

<TwoOptionsLayout@BaseLayout>:
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "Number of tickets distributed"
        BigLabel:
            id: labelCount
            text: str(app.count)
        OptionButton:
            id: buttonOptionA
            text: str(app.config["options"]["option_a"]["name"])
            on_press: app.click_option_a()
        OptionButton:
            id: buttonOptionB
            text: str(app.config["options"]["option_b"]["name"])
            on_press: app.click_option_b()
        Label:
            id: labelNextNumber
            text: "Next number"
        BoxLayout:
            orientation: "horizontal"
            BigLabel:
                id: labelNextNumberOptionA
                text: str(app.nextOptionA)
            BigLabel:
                id: labelNextNumberOptionB
                text: str(app.nextOptionB)
    QRWidget

<ThreeOptionsLayout@BaseLayout>:
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "Number of tickets distributed"
        BigLabel:
            id: labelCount
            text: str(app.count)
        OptionButton:
            id: buttonOptionA
            text: str(app.config["options"]["option_a"]["name"])
            on_press: app.click_option_a()
        OptionButton:
            id: buttonOptionB
            text: str(app.config["options"]["option_b"]["name"])
            on_press: app.click_option_b()
        OptionButton:
            id: buttonOptionC
            text: str(app.config["options"]["option_c"]["name"])
            on_press: app.click_option_c()
        Label:
            id: labelNextNumber
            text: "Next number"
        BoxLayout:
            orientation: "horizontal"
            BigLabel:
                id: labelNextNumberOptionA
                text: str(app.nextOptionA)
            BigLabel:
                id: labelNextNumberOptionB
                text: str(app.nextOptionB)
            BigLabel:
                id: labelNextNumberOptionC
                text: str(app.nextOptionC)
    QRWidget

<FourOptionsLayout@BaseLayout>:
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "Number of tickets distributed"
        BigLabel:
            id: labelCount
            text: str(app.count)
        OptionButton:
            id: buttonOptionA
            text: str(app.config["options"]["option_a"]["name"])
            on_press: app.click_option_a()
        OptionButton:
            id: buttonOptionB
            text: str(app.config["options"]["option_b"]["name"])
            on_press: app.click_option_b()
        OptionButton:
            id: buttonOptionC
            text: str(app.config["options"]["option_c"]["name"])
            on_press: app.click_option_c()
        OptionButton:
            id: buttonOptionD
            text: str(app.config["options"]["option_d"]["name"])
            on_press: app.click_option_d()
        Label:
            id: labelNextNumber
            text: "Next number"
        BoxLayout:
            orientation: "horizontal"
            BigLabel:
                id: labelNextNumberOptionA
                text: str(app.nextOptionA)
            BigLabel:
                id: labelNextNumberOptionB
                text: str(app.nextOptionB)
            BigLabel:
                id: labelNextNumberOptionC
                text: str(app.nextOptionC)
            BigLabel:
                id: labelNextNumberOptionD
                text: str(app.nextOptionD)
    QRWidget

<FiveOptionsLayout@BaseLayout>:
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "Number of tickets distributed"
        BigLabel:
            id: labelCount
            text: str(app.count)
        OptionButton:
            id: buttonOptionA
            text: str(app.config["options"]["option_a"]["name"])
            on_press: app.click_option_a()
        OptionButton:
            id: buttonOptionB
            text: str(app.config["options"]["option_b"]["name"])
            on_press: app.click_option_b()
        OptionButton:
            id: buttonOptionC
            text: str(app.config["options"]["option_c"]["name"])
            on_press: app.click_option_c()
        OptionButton:
            id: buttonOptionD
            text: str(app.config["options"]["option_d"]["name"])
            on_press: app.click_option_d()
        OptionButton:
            id: buttonOptionE
            text: str(app.config["options"]["option_e"]["name"])
            on_press: app.click_option_e()
        Label:
            id: labelNextNumber
            text: "Next number"
        BoxLayout:
            orientation: "horizontal"
            BigLabel:
                id: labelNextNumberOptionA
                text: str(app.nextOptionA)
            BigLabel:
                id: labelNextNumberOptionB
                text: str(app.nextOptionB)
            BigLabel:
                id: labelNextNumberOptionC
                text: str(app.nextOptionC)
            BigLabel:
                id: labelNextNumberOptionD
                text: str(app.nextOptionD)
            BigLabel:
                id: labelNextNumberOptionE
                text: str(app.nextOptionE)
    QRWidget

<SixOptionsLayout@BaseLayout>:
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "Number of tickets distributed"
        BigLabel:
            id: labelCount
            text: str(app.count)
        OptionButton:
            id: buttonOptionA
            text: str(app.config["options"]["option_a"]["name"])
            on_press: app.click_option_a()
        OptionButton:
            id: buttonOptionB
            text: str(app.config["options"]["option_b"]["name"])
            on_press: app.click_option_b()
        OptionButton:
            id: buttonOptionC
            text: str(app.config["options"]["option_c"]["name"])
            on_press: app.click_option_c()
        OptionButton:
            id: buttonOptionD
            text: str(app.config["options"]["option_d"]["name"])
            on_press: app.click_option_d()
        OptionButton:
            id: buttonOptionE
            text: str(app.config["options"]["option_e"]["name"])
            on_press: app.click_option_e()
        OptionButton:
            id: buttonOptionF
            text: str(app.config["options"]["option_f"]["name"])
            on_press: app.click_option_f()
        Label:
            id: labelNextNumber
            text: "Next number"
        BoxLayout:
            orientation: "horizontal"
            BigLabel:
                id: labelNextNumberOptionA
                text: str(app.nextOptionA)
            BigLabel:
                id: labelNextNumberOptionB
                text: str(app.nextOptionB)
            BigLabel:
                id: labelNextNumberOptionC
                text: str(app.nextOptionC)
            BigLabel:
                id: labelNextNumberOptionD
                text: str(app.nextOptionD)
            BigLabel:
                id: labelNextNumberOptionE
                text: str(app.nextOptionE)
            BigLabel:
                id: labelNextNumberOptionF
                text: str(app.nextOptionF)
    QRWidget

<SevenOptionsLayout@BaseLayout>:
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "Number of tickets distributed"
        BigLabel:
            id: labelCount
            text: str(app.count)
        OptionButton:
            id: buttonOptionA
            text: str(app.config["options"]["option_a"]["name"])
            on_press: app.click_option_a()
        OptionButton:
            id: buttonOptionB
            text: str(app.config["options"]["option_b"]["name"])
            on_press: app.click_option_b()
        OptionButton:
            id: buttonOptionC
            text: str(app.config["options"]["option_c"]["name"])
            on_press: app.click_option_c()
        OptionButton:
            id: buttonOptionD
            text: str(app.config["options"]["option_d"]["name"])
            on_press: app.click_option_d()
        OptionButton:
            id: buttonOptionE
            text: str(app.config["options"]["option_e"]["name"])
            on_press: app.click_option_e()
        OptionButton:
            id: buttonOptionF
            text: str(app.config["options"]["option_f"]["name"])
            on_press: app.click_option_f()
        OptionButton:
            id: buttonOptionG
            text: str(app.config["options"]["option_g"]["name"])
            on_press: app.click_option_g()
        Label:
            id: labelNextNumber
            text: "Next number"
        BoxLayout:
            orientation: "horizontal"
            BigLabel:
                id: labelNextNumberOptionA
                text: str(app.nextOptionA)
            BigLabel:
                id: labelNextNumberOptionB
                text: str(app.nextOptionB)
            BigLabel:
                id: labelNextNumberOptionC
                text: str(app.nextOptionC)
            BigLabel:
                id: labelNextNumberOptionD
                text: str(app.nextOptionD)
            BigLabel:
                id: labelNextNumberOptionE
                text: str(app.nextOptionE)
            BigLabel:
                id: labelNextNumberOptionF
                text: str(app.nextOptionF)
            BigLabel:
                id: labelNextNumberOptionG
                text: str(app.nextOptionG)
    QRWidget

<EightOptionsLayout@BaseLayout>:
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "Number of tickets distributed"
        BigLabel:
            id: labelCount
            text: str(app.count)
        OptionButton:
            id: buttonOptionA
            text: str(app.config["options"]["option_a"]["name"])
            on_press: app.click_option_a()
        OptionButton:
            id: buttonOptionB
            text: str(app.config["options"]["option_b"]["name"])
            on_press: app.click_option_b()
        OptionButton:
            id: buttonOptionC
            text: str(app.config["options"]["option_c"]["name"])
            on_press: app.click_option_c()
        OptionButton:
            id: buttonOptionD
            text: str(app.config["options"]["option_d"]["name"])
            on_press: app.click_option_d()
        OptionButton:
            id: buttonOptionE
            text: str(app.config["options"]["option_e"]["name"])
            on_press: app.click_option_e()
        OptionButton:
            id: buttonOptionF
            text: str(app.config["options"]["option_f"]["name"])
            on_press: app.click_option_f()
        OptionButton:
            id: buttonOptionG
            text: str(app.config["options"]["option_g"]["name"])
            on_press: app.click_option_g()
        OptionButton:
            id: buttonOptionH
            text: str(app.config["options"]["option_h"]["name"])
            on_press: app.click_option_h()
        Label:
            id: labelNextNumber
            text: "Next number"
        BoxLayout:
            orientation: "horizontal"
            BigLabel:
                id: labelNextNumberOptionA
                text: str(app.nextOptionA)
            BigLabel:
                id: labelNextNumberOptionB
                text: str(app.nextOptionB)
            BigLabel:
                id: labelNextNumberOptionC
                text: str(app.nextOptionC)
            BigLabel:
                id: labelNextNumberOptionD
                text: str(app.nextOptionD)
            BigLabel:
                id: labelNextNumberOptionE
                text: str(app.nextOptionE)
            BigLabel:
                id: labelNextNumberOptionF
                text: str(app.nextOptionF)
            BigLabel:
                id: labelNextNumberOptionG
                text: str(app.nextOptionG)
            BigLabel:
                id: labelNextNumberOptionH
                text: str(app.nextOptionH)
    QRWidget

<NineOptionsLayout@BaseLayout>:
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "Number of tickets distributed"
        BigLabel:
            id: labelCount
            text: str(app.count)
        OptionButton:
            id: buttonOptionA
            text: str(app.config["options"]["option_a"]["name"])
            on_press: app.click_option_a()
        OptionButton:
            id: buttonOptionB
            text: str(app.config["options"]["option_b"]["name"])
            on_press: app.click_option_b()
        OptionButton:
            id: buttonOptionC
            text: str(app.config["options"]["option_c"]["name"])
            on_press: app.click_option_c()
        OptionButton:
            id: buttonOptionD
            text: str(app.config["options"]["option_d"]["name"])
            on_press: app.click_option_d()
        OptionButton:
            id: buttonOptionE
            text: str(app.config["options"]["option_e"]["name"])
            on_press: app.click_option_e()
        OptionButton:
            id: buttonOptionF
            text: str(app.config["options"]["option_f"]["name"])
            on_press: app.click_option_f()
        OptionButton:
            id: buttonOptionG
            text: str(app.config["options"]["option_g"]["name"])
            on_press: app.click_option_g()
        OptionButton:
            id: buttonOptionH
            text: str(app.config["options"]["option_h"]["name"])
            on_press: app.click_option_h()
        OptionButton:
            id: buttonOptionI
            text: str(app.config["options"]["option_i"]["name"])
            on_press: app.click_option_i()
        Label:
            id: labelNextNumber
            text: "Next number"
        BoxLayout:
            orientation: "horizontal"
            BigLabel:
                id: labelNextNumberOptionA
                text: str(app.nextOptionA)
            BigLabel:
                id: labelNextNumberOptionB
                text: str(app.nextOptionB)
            BigLabel:
                id: labelNextNumberOptionC
                text: str(app.nextOptionC)
            BigLabel:
                id: labelNextNumberOptionD
                text: str(app.nextOptionD)
            BigLabel:
                id: labelNextNumberOptionE
                text: str(app.nextOptionE)
            BigLabel:
                id: labelNextNumberOptionF
                text: str(app.nextOptionF)
            BigLabel:
                id: labelNextNumberOptionG
                text: str(app.nextOptionG)
            BigLabel:
                id: labelNextNumberOptionH
                text: str(app.nextOptionH)
            BigLabel:
                id: labelNextNumberOptionI
                text: str(app.nextOptionI)
    QRWidget

<TenOptionsLayout@BaseLayout>:
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "Number of tickets distributed"
        BigLabel:
            id: labelCount
            text: str(app.count)
        OptionButton:
            id: buttonOptionA
            text: str(app.config["options"]["option_a"]["name"])
            on_press: app.click_option_a()
        OptionButton:
            id: buttonOptionB
            text: str(app.config["options"]["option_b"]["name"])
            on_press: app.click_option_b()
        OptionButton:
            id: buttonOptionC
            text: str(app.config["options"]["option_c"]["name"])
            on_press: app.click_option_c()
        OptionButton:
            id: buttonOptionD
            text: str(app.config["options"]["option_d"]["name"])
            on_press: app.click_option_d()
        OptionButton:
            id: buttonOptionE
            text: str(app.config["options"]["option_e"]["name"])
            on_press: app.click_option_e()
        OptionButton:
            id: buttonOptionF
            text: str(app.config["options"]["option_f"]["name"])
            on_press: app.click_option_f()
        OptionButton:
            id: buttonOptionG
            text: str(app.config["options"]["option_g"]["name"])
            on_press: app.click_option_g()
        OptionButton:
            id: buttonOptionH
            text: str(app.config["options"]["option_h"]["name"])
            on_press: app.click_option_h()
        OptionButton:
            id: buttonOptionI
            text: str(app.config["options"]["option_i"]["name"])
            on_press: app.click_option_i()
        OptionButton:
            id: buttonOptionJ
            text: str(app.config["options"]["option_j"]["name"])
            on_press: app.click_option_j()
        Label:
            id: labelNextNumber
            text: "Next number"
        BoxLayout:
            orientation: "horizontal"
            BigLabel:
                id: labelNextNumberOptionA
                text: str(app.nextOptionA)
            BigLabel:
                id: labelNextNumberOptionB
                text: str(app.nextOptionB)
            BigLabel:
                id: labelNextNumberOptionC
                text: str(app.nextOptionC)
            BigLabel:
                id: labelNextNumberOptionD
                text: str(app.nextOptionD)
            BigLabel:
                id: labelNextNumberOptionE
                text: str(app.nextOptionE)
            BigLabel:
                id: labelNextNumberOptionF
                text: str(app.nextOptionF)
            BigLabel:
                id: labelNextNumberOptionG
                text: str(app.nextOptionG)
            BigLabel:
                id: labelNextNumberOptionH
                text: str(app.nextOptionH)
            BigLabel:
                id: labelNextNumberOptionI
                text: str(app.nextOptionI)
            BigLabel:
                id: labelNextNumberOptionJ
                text: str(app.nextOptionJ)
    QRWidget


<BaseLayout>:
    orientation: "horizontal"
    currentNumberText: app.currentNumber


<QRWidget@BoxLayout>:
    orientation: "vertical"
    Label:
        id: "labelCurrentNumberInfo"
        text: "Your number is"
        size_hint: 1, 0.5
    BigLabel:
        id: "labelCurrentNumber"
        text: str(app.currentNumber)
        font_size: 50
    QRCodeWidget:
        id: "qrNumber"
        data: app.currentNumber
        show_border: False
        background_color: app.color["qrBackground"]
    Label:
        id: "labelInstructions"
        text: "Scan this QR code or take a photograph of it"
        size_hint: 1, 0.7

<OptionButton@Button>:
    color: app.color["text"]
    font_size: 20

<BigLabel@Label>:
    color: app.color["text"]
    font_size: 50
    size_hint: 1, 1.75
